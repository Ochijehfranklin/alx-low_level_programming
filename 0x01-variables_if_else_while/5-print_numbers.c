#include <stdio.h>

/**
 * main - This is the entry point
 * description: 'Printall single digits of base ten'
 * Return: Always 0
 */

int main(void)

{
	int x;

	for (x = 0; x <= 9; x++)
	{
		printf("%d", x);
	}
	printf("\n");
	return (0);
}
